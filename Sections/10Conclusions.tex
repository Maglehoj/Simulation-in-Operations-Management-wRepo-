\section{Conclusions and Insights}
\label{sec:conclusions}

% ~0.75 page. Managerial takeaways, tied to the KPIs.
\begin{itemize}\setlength\itemsep{1pt}
  \item The primary constraint is \textbf{drying} (highest required unit-hours
        and simulated utilisation), with the \textbf{cleaning team} a close
        second; packaging, despite the largest absolute workload, is third
        because it runs on three parallel lines.
  \item The most cost-effective improvement is \textbf{[relieving drying]},
        which raises weekly throughput by \textbf{[x]\%} (95\% CI [\,]) and
        \emph{also} unloads the cleaning team - a better return than the
        capital-heavy packaging-line expansion the 2030 plan seeks to defer.
  \item Sequencing batches by product reduces changeover load on the cleaning
        team, with effect \textbf{[\,]}.
  \item Towards 2030, the line sustains up to \textbf{[N]} batches/week before
        backlog grows unbounded; the best improvement combination extends this to
        \textbf{[M]}.
  \item Industry~4.0 press sensors (scrap 10\%$\rightarrow$4\%) yield
        \textbf{[\,]} additional good tablets/week, worth \textbf{[\,]}.
\end{itemize}
