\section{Conceptual Modelling}
\label{sec:conceptual}

One process-batch flows as a single agent through six operations, becoming a
specific product only at coating. The plan gives each batch as
\SI{150}{\kilo\gram} of active-ingredient powder blend; \SI{75}{\kilo\gram} of
excipients is added in premix, giving a \SI{225}{\kilo\gram} batch of
\num{300000} tablets.
Figure~\ref{fig:flow} is the high-level flow chart; Figure~\ref{fig:activity} is
the activity diagram (Rossetti notation~\cite{rossetti2016}): circles are queues, rectangles are
activities with their delay distribution, small circles are resources, and
dotted lines show the seizing/releasing of resources.

\subsection{System flow chart}
\label{sec:flow}
\begin{figure}[H]
\centering
\resizebox{0.40\textwidth}{!}{%
\begin{tikzpicture}[
  >=Stealth, font=\scriptsize, node distance = 5mm and 8mm,
  pr/.style={rectangle, draw, align=center, minimum width=30mm, minimum height=8mm},
  tm/.style={rectangle, draw, rounded corners=4pt, fill=black!6, align=center, minimum width=24mm, minimum height=7mm, font=\scriptsize\bfseries},
  dd/.style={diamond, draw, aspect=2, align=center, font=\tiny, inner sep=1pt},
  sc/.style={rectangle, draw, dashed, align=center, font=\tiny, minimum width=14mm, minimum height=6mm},
  a/.style={->, thick}]
\node[tm] (s) {Source (plan)};
\node[pr, below=of s] (pm) {Premix (old\,$\parallel$\,new)};
\node[pr, below=of pm] (gr) {Granulation};
\node[pr, below=of gr] (dr) {Drying};
\node[pr, below=of dr] (cp) {Compression};
\node[dd, below=of cp] (cd) {CoreTab?};
\node[pr, below right=5mm and 4mm of cd] (ct) {Coating};
\node[pr, below=22mm of cd] (pk) {Packaging};
\node[tm, below=of pk] (sh) {Ship (weekly)};
\node[sc, left=8mm of pm] (sp) {scrap};
\node[sc, right=8mm of pk] (sp2) {scrap};
\draw[a](s)--(pm);\draw[a](pm)--(gr);\draw[a](gr)--(dr);\draw[a](dr)--(cp);\draw[a](cp)--(cd);
\draw[a](cd.east)-|(ct.north) node[pos=.25,above]{no};
\draw[a](ct)|-(pk.east);
\draw[a](cd.west)--++(-20mm,0) node[pos=.5,above]{yes} |-(pk.west);
\draw[a](pk)--(sh);
\draw[a,dashed](pm)--(sp) node[pos=.5,above,font=\tiny]{fail};
\draw[a,dashed](pk)--(sp2) node[pos=.5,above,font=\tiny]{defect};
\end{tikzpicture}%
}
\caption{Simple flow chart of the FermaCore line. CoreTab skips coating; a premix
failure or pre-packaging inspection scraps material.}
\label{fig:flow}
\end{figure}

\subsection{Activity diagram}
\label{sec:activity}
\begin{figure}[H]
\centering
\resizebox{0.38\textwidth}{!}{%
\begin{tikzpicture}[
  >=Stealth, font=\scriptsize, node distance = 4mm and 14mm,
  qu/.style  = {circle, draw, align=center, minimum size=11mm, inner sep=0.4pt},
  ac/.style  = {rectangle, draw, fill=blue!4, align=center, minimum height=9mm, minimum width=26mm},
  rs/.style  = {circle, draw, fill=black!8, align=center, minimum size=9mm, inner sep=0.4pt, font=\tiny},
  dc/.style  = {diamond, draw, aspect=1.6, align=center, inner sep=0.4pt, font=\tiny},
  fl/.style  = {->, thick},
  sr/.style  = {densely dotted, gray!70},
  zz/.style  = {-{Stealth}, thick, decorate, decoration={zigzag, segment length=2.2mm, amplitude=0.9mm, pre length=1mm, post length=2mm}}]
\node (create) {\itshape Create: batch (plan)};
\node[qu, below=6mm of create] (q1) {Premix\\queue};
\node[ac, below=of q1] (premix) {Premix\\ New: tri(3.5,4,4.5) h\\ Old: tri(4.5,5.5,6.5) h};
\node[qu, below=of premix] (q2) {Gran.\\queue};
\node[ac, below=of q2] (gran) {Granulate\\ N(1,0.1) h};
\node[qu, below=of gran] (q3) {Drying\\queue};
\node[ac, below=of q3] (dry) {Dry $+$ clean\\ N(4,0.39) h $+$ 2 h};
\node[qu, below=of dry] (q4) {Compr.\\queue};
\node[ac, below=of q4] (comp) {Compress\\ 2 h};
\node[dc, below=of comp] (core) {CoreTab?};
\node[ac, below=of core] (coat) {Coat $+$ clean\\ tri(1.5,2,2.5)$+$1 h};
\node[qu, below=of coat] (q5) {Pack.\\queue};
\node[ac, below=of q5] (pack) {Setup $+$ Package\\ 30k tab/h \,(scrap)};
\node[below=6mm of pack] (dispose) {\itshape Dispose: ship};
\node[rs, right=of premix] (rb) {Blenders (2)};
\node[rs, right=of gran]   (rg) {Gran. (1)};
\node[rs, right=of dry]    (rd) {Dryer (1)};
\node[rs, right=of comp]   (rp) {Press (3)};
\node[rs, right=of coat]   (rc) {Coater (1)};
\node[rs, right=of pack]   (rl) {Line (3)};
\node[rs, left=22mm of comp, fill=black!16] (clt) {Cleaning\\team};
\draw[zz] (create) -- (q1);
\draw[fl] (q1)--(premix);\draw[fl] (premix)--(q2);\draw[fl] (q2)--(gran);
\draw[fl] (gran)--(q3);\draw[fl] (q3)--(dry);\draw[fl] (dry)--(q4);
\draw[fl] (q4)--(comp);\draw[fl] (comp)--(core);
\draw[fl] (core) -- node[right]{no} (coat);
\draw[fl] (coat)--(q5);\draw[fl] (q5)--(pack);
\draw[zz] (pack) -- (dispose);
\draw[fl] (core.west) -- ++(-9mm,0) node[above,pos=0.55]{yes} |- (q5.west);
\draw[sr] (premix)--(rb);\draw[sr] (gran)--(rg);\draw[sr] (dry)--(rd);
\draw[sr] (comp)--(rp);\draw[sr] (coat)--(rc);\draw[sr] (pack)--(rl);
\draw[sr] (clt) -- node[above,sloped,font=\tiny]{seize/release} (dry.west);
\draw[sr] (clt) -- (coat.west);
\draw[sr] (clt) |- (pack.west);
\end{tikzpicture}%
}
\caption{Activity diagram (Rossetti notation). Circles are queues, rectangles are
activities with delay distribution, small circles are resources; dotted lines
indicate seizing/releasing. The shared cleaning team is seized at the drying
clean, coating clean and packaging setup.}
\label{fig:activity}
\end{figure}