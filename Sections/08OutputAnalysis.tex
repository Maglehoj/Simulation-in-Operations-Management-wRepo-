\section{Output Analysis}
\label{sec:output}

This is a \textbf{terminating} simulation: the system starts empty-and-idle and
ends when the 50\textsuperscript{th} batch ships, so no warm-up is deleted.
Each scenario is run as $n$ independent replications (vary the random seed) in a
\texttt{Parameters Variation} experiment.

\paragraph{Number of replications.} We size $n$ with the half-width method on
the headline KPI (weekly throughput). From $n_0=10$ pilot replications,
\begin{equation}
  n \;\ge\; \left(\frac{t_{n_0-1,\,1-\alpha/2}\,s_0}{\varepsilon}\right)^{\!2},
  \label{eq:halfwidth}
\end{equation}
targeting a relative precision $\varepsilon \le \SI{5}{\percent}$ of the mean at
$\alpha=0.05$.

\begin{table}[H]
\centering
\caption{Replication sizing from pilot runs (fill from your pilot output).}
\label{tab:reps}
\begin{tabularx}{\textwidth}{X r r r r}
\toprule
KPI & mean $\bar x$ & st.dev $s_0$ & half-width & required $n$ \\
\midrule
Weekly throughput & [\,] & [\,] & [\,] & [\,] \\
Packaging utilisation & [\,] & [\,] & [\,] & [\,] \\
\bottomrule
\end{tabularx}
\end{table}
% TODO: report final n and the resulting CIs.
